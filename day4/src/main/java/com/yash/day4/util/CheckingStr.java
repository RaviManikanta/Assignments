package com.yash.day4.util;

public class CheckingStr {
	String[] chars = st.toStringArray();
		StringBuilder di = new StringBuilder();
		for(char st : chars){
			if(Character.isDigit(st)){
				di.append(st);
              }
		}
		}
